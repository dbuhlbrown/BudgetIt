#Helps us graph the points, just to visualize the data a bit better
class GraphHelper:

	def __init__( self ):

		self.labels = []
		self.maxX = 0
		self.maxY = 0
		self.points = []

	def create_points( self ):	

		return self.points